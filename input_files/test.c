int global1;

float calculateAvg(float num1, float num2, int num){
     bool condition;
     while(temp <= 3){
          if(condition == condition){
               for(fora = 1; fora < 3; fora = fora + 1){
                    if(condition != temp){
                         fora = fora + fora;
                    }
                    int ifa = num2;
                    ifa = fora;
               }
          }
          else{
               else_ = num1;
               num1 = num2;
               num2 = (temp + 2) * (4 - num2);
          }   
     }

     return ((num1 + num2) / 2);
}

float global2;